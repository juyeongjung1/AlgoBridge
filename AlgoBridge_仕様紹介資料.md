# AlgoBridge 仕様紹介資料

## 1. 概要

AlgoBridgeは、Java研修前の受講者を対象にした、アルゴリズム入門用のブラウザ学習システムです。

受講者はJavaの構文を直接書く前に、自然な日本語で書かれた処理の文章ブロックを組み立てます。組み立てた処理を実行し、出力結果・変数の変化・フローチャート・Javaコードとの対応を確認することで、アルゴリズムの考え方からJavaの記述へ段階的に進めます。

## 2. 解決したい課題

Javaの学習を始めたばかりの受講者は、構文の書き方に意識が集中しやすく、「どの処理を、どの順番で、どの階層に置くべきか」を理解しないままコードを書いてしまうことがあります。

また、フローチャートを作成するだけでは、処理を実行したときの出力結果や変数の変化を確認できません。AlgoBridgeでは、処理を組み立てる体験と実行結果の確認を一体化し、受講者が自分の考えたアルゴリズムを検証できるようにします。

## 3. 学習の流れ

1. 問題文と期待する出力結果を読む
2. 必要な処理を文章ブロックから選ぶ
3. 実行順と階層構造を考えながら組み立てる
4. 実行して、出力結果と変数の変化を確認する
5. 同じ処理をフローチャートとJavaコードで確認する

## 4. 主な機能

### 4.1 文章ブロックによる組立て

- 入力・処理・判断・繰り返し・出力を、役割に応じた図形で表示します。
- ブロックはドラッグ＆ドロップで組み立てます。
- 繰り返しと条件分岐では、ブロックの内側に処理を配置できます。
- 正解に不要な、もっともらしい誤答ブロックも表示します。受講者は単なる並べ替えではなく、問題文から必要な処理を考えて選びます。
- 入力値を受け取るブロックと、変数を宣言する準備ブロックは、処理に影響しない範囲で順不同に配置できます。

### 4.2 実行結果とトレース表

- 正しい構造で組み立てられた場合のみ、JavaScriptで処理をシミュレーションします。
- 出力結果を画面に表示します。
- 繰り返し問題では、ループカウンタ変数や合計用変数などの値の変化をトレース表で確認できます。
- 受講者は「なぜその結果になるのか」を、途中の変数の変化から理解できます。

### 4.3 フローチャートとJavaコードの対応

- 実行成功後、フローチャートとJavaコードを切り替えて表示できます。
- Javaコードの行を選択すると、対応する文章ブロックが分かります。
- Java未経験者向けに、`int`、`String`、`if`、`for`、代入、比較演算子などの基本構文を簡潔に説明します。

### 4.4 ヒントとフィードバック

- 組み立てが未完成の場合は、正解を直接示さず、「必要な処理ブロックがまだ揃っていません」などのフィードバックを表示します。
- ヒントは、まず考え方を促す簡単なヒントを表示します。
- 受講者が「具体的なヒントを見る」を選択した場合だけ、より具体的なヒントを表示します。
- 誤答の選択肢についても、なぜ問題文に合わないかを考えられる構成です。

### 4.5 正解後の振り返り

- 正解時には達成感を高めるポップアップを表示します。
- 正解後は、実行結果・変数の変化・フローチャート・Javaコード・詳細解説をいつでも確認できます。
- 正解済みの情報はブラウザ内に保存され、問題を切り替えた後も振り返りができます。
- ヘッダーには、全30問中の正解数を表示します。必要に応じて正解状態をリセットできます。

### 4.6 講師モード

- ヘッダーからローカルパスワードで講師モードへ切り替えられます。
- 講師モードでは、組み立て途中でも正解例の実行結果を確認できます。
- 講義で正解例を投影し、フローチャート・Javaコード・解説を使って説明できます。
- 講師モードの状態はブラウザ内に保存され、受講者モードへ戻すこともできます。

## 5. 搭載する問題構成

現在は、難易度を段階的に上げる30問を搭載しています。

| 分野 | 主な問題例 |
| --- | --- |
| 順次 | 氏名の整形、三角形の面積、BMI、保険料の請求額 |
| 選択 | 偶数・奇数、2つの値の比較、曜日、挨拶、うるう年 |
| 繰り返し | 1からnまでの合計、偶数の合計、FizzBuzz、年間保険料 |
| 配列 | 配列の合計・平均・最大値・件数・検索 |
| 配列操作 | 値の交換、バブルソートの1回分、バブルソート |
| ネストした選択 | 事故受付、継続契約の割引判定 |

保険会社での研修利用を想定し、保険料、契約状態、証券番号、事故受付、継続契約などの業務イメージにつながる題材も取り入れています。

## 6. 代表的な学習例：1からnまでの合計

問題文：整数`n`が与えられたとき、1から`n`までの整数の合計を求めて出力します。

組み立てる処理の例：

1. 入力値を受け取り、変数`n`を宣言すると同時に設定する
2. 変数`sum`を宣言する
3. 変数`sum`に初期値として`0`を設定する
4. ループカウンタ変数`i`を1から`n`まで、1ずつ増やしながら繰り返す
   - 変数`sum`にループカウンタ変数`i`の値を加える
5. 変数`sum`の値を出力する

この問題では、`sum`を使う前に宣言する理由、初期値が`0`である理由、ループカウンタ変数`i`が1から始まる理由、繰り返しの中で`sum`に`i`を加える理由を、正解後の解説で確認できます。

## 7. 研修での活用イメージ

### 講義の導入

講師モードで正解例を表示し、文章ブロック、フローチャート、Javaコードの対応を説明します。

### 個人演習

受講者は自分でブロックを選択・配置し、実行結果やヒントを使いながら解答します。

### 振り返り

正解後の詳細解説、トレース表、Javaコードの構文説明を用いて、なぜその処理になるのかを言語化します。

### Java研修への接続

文章ブロックで理解した入力、変数、条件分岐、繰り返し、配列の考え方を、Javaの構文として記述する演習へつなげます。

## 8. 技術・運用上の特徴

- HTML、CSS、JavaScriptのみで動作する静的Webアプリです。
- Javaのコンパイル環境やサーバーを必要としません。
- 判定と実行シミュレーションはブラウザ内で行います。
- 正解状況や講師モードの状態は、利用端末のブラウザ内に保存されます。
- 問題は問題単位の定義として管理しており、今後も問題文・ブロック・判定条件・実行処理を追加できます。

## 9. 今後の展開案

- 研修カリキュラムに合わせた問題の追加・入れ替え
- 問題ごとの到達状況やヒント利用状況の可視化
- 講師向けの演習進行ガイドの追加
- Javaのコード記述演習との連携
- 保険業務のケーススタディを題材にした応用問題の追加

## 10. まとめ

AlgoBridgeは、受講者がJavaの文法を覚える前に、処理の順序・条件分岐・繰り返し・変数の役割を体験的に理解するための学習システムです。

文章ブロックの組立て、実行結果、トレース表、フローチャート、Javaコードを一貫して確認できるため、アルゴリズム理解からJava研修への自然な橋渡しを実現します。
